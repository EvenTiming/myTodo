import { createStore } from 'vuex';

export default createStore({
  state() {
    return {
      // 定义全局状态
    };
  },
  mutations: {
    // 定义修改全局状态的方法
  },
  actions: {
    // 定义全局的异步操作方法
  },
  getters: {
    // 定义获取全局状态的方法
  },
});
